/**
 * Blog post redirects map
 *
 * Use this to redirect old blog post slugs to new ones (e.g., after renaming a post).
 * These are permanent (301) redirects for SEO purposes.
 *
 *
 * @example
 * export const blogRedirects: Record<string, string> = {
 *   'old-turkish-post-name': 'new-turkish-post-name',
 * };
 */
export const blogRedirects: Record<string, string> = {
	'nas%C4%B1l-gormek-istedigimizi-goruruz': 'neyi-gormek-istersek-onu-goruruz'
};
